1.def count_vowels(text):
      count_vowel=0
      for letter in text:
         if letter in "aeiou" or letter in "AEIOU" :
             count_vowel+=1
      return count_vowel
  text=input("Enter a string:")
  count_vowel=count_vowels(text)
  print(f"Count of vowels:{count_vowel}")

2.def count_consonants(text):
      count=0
      alphabets = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ"
      for letter in text:
          if letter in alphabets and letter not in "aeiouAEIOU":
               count+=1
      return count
  text=input("Enter a string:")
  count=count_consonants(text)
  print(f"Count of consonants:{count}")

3.def count_digits(text):
      count=0
      num="1234567890"
      alphabets = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ"
      for letter in text:
           if letter in num and letter not in alphabets:
                     count+=1
      return count
  text=input("Enter a string:")
  count=count_digits(text)
  print(f"Count of digits:{count}")


4.def count_spaces(text):
      count=0
      num="1234567890"
      alphabets = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ"
      for letter in text:
           if letter not in num and letter not in alphabets and letter==" ":
                     count+=1
      return count
  text=input("Enter a string:")
  count=count_spaces(text)
  print(f"Count of spaces:{count}")

5.def reverse_string(text):
      reverse=""
      for letter in text:
          reverse=letter+reverse
      return reverse
  text=input("Enter a string:")
  reverse=reverse_string(text)
  print(f"Reversed string:{reverse}")

s6.def is_palindrome(text):
      reverse=""
      for letter in text:
          reverse=letter+reverse
      if reverse==text:
         return True
      else:  
         return False
  text=input("Enter a string:")
  result=is_palindrome(text)
  print(f"The string is palindrome:{result}")


s7.def to_upper(text):
      text2=""
      text3=""
      final=""
      alphabets = "abcdefghijklmnopqrstuvwxyz"
      for letter in text:
        if letter in alphabets:
           text2=(ord(letter))
           text3=text2-32
           final+=(chr(text3))
        else:
           final+=letter
      return final
  text=input("Enter a string:")
  final=to_upper(text)
  print(f"The string in upper:{final}")


s8.def to_lower(text):
      text2=""
      text3=""
      final=""
      alphabets = "ABCDEFGHIJKLMNOPQRSTUVWXYZ"
      for letter in text:
        if letter in alphabets:
           text2=(ord(letter))
           text3=text2+32
           final+=(chr(text3))
        else:
           final+=letter
      return final
  text=input("Enter a string:")
  final=to_lower(text)
  print(f"The string in lower:{final}")

s9.def remove_spaces(text):
      removed=""
      for letter in text:
        if letter!=" ":
          removed+=letter
      return removed
  text=input("Enter a string:")
  removed=remove_spaces(text)
  print(f"The spaces in the string removed:{removed}")
 
s10.def count_character(text, ch):
       count=0
       for letter in text:
           if letter == ch:
              count+=1
       return count
   text=input("Enter a string:")
   ch=input("Enter a character:")
   count=count_character(text,ch)
   print(f"The number of times ch appear in the string:{count}")


s11.def count_uppercase(text):
       count=0
       alphabets = "ABCDEFGHIJKLMNOPQRSTUVWXYZ"
       for letter in text:
           if letter in alphabets:
              count+=1
       return count
   text=input("Enter a string:")
   count=count_uppercase(text)
   print(f"The count of upper in the string:{count}")
 
s12.def count_lowercase(text):
       count=0
       alphabets = "abcdefghijklmnopqrstuvwxyz"
       for letter in text:
           if letter in alphabets:
              count+=1
       return count
   text=input("Enter a string:")
   count=count_lowercase(text)
   print(f"The count of lower in the string:{count}")
 
      
s13.def toggle_case(text):
        letter1=""
        letter2=""
        final=""
        lowercase="abcdefghijklmnopqrstuvwxyz"
        uppercase="ABCDEFGHIJKLMNOPQRSTUVWXYZ"
        for letter in text:
            if letter in lowercase:
               letter1=ord(letter)
               letter2=letter1-32
               final+=chr(letter2)
            elif letter in uppercase:
               letter1=ord(letter)
               letter2=letter1+32
               final+=chr(letter2)
            else:
               final+=letter
        return final
    text=input("Enter a string:")
    final=toggle_case(text)
    print(f"The toggled string is:{final}")
 
s14.def remove_digits(text):
        digit="1234567890"
        final=""
        for letter in text:
            if letter not in digit:
               final+=letter
        return final
    text=input("Enter a string:")
    final=remove_digits(text)
    print(f"The updated string is:{final}")


s15.def count_special(text): 
        digit="1234567890"
        alphabets = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ"
        count=0
        for letter in text:
            if letter not in alphabets and letter not in digit and letter!=" ":
               count+=1
        return count
    text=input("Enter a string:")
    count=count_special(text)
    print(f"The count of special character in a string:{count}")

s16.def first_digit(text):
        digit="0123456789"
        for letter in text:
            if letter in digit:
                return letter
        return None
    text=input("Enter a string:")
    result=first_digit(text)
    print(f"The first_digit in the string:{result}")

s17.def last_digit(text):
        digit="0123456789"
        last=None
        for letter in text:
            if letter in digit:
                last= letter
        return last
    text=input("Enter a string:")
    result=last_digit(text)
    print(f"The last_digit in the string:{result}")

 s18.def remove_vowels(text):
         vowel="aeiouAEIOU"
         final=""
         for letter in text:
             if letter not in vowel:
                final+=letter
         return final
     text=input("Enter a string:")
     final=remove_vowels(text)
     print(f"The updated string:{final}")


s19.def count_words(text):
        count=0
        for i in range(len(text)):
            if text[i]!=" " and( i == 0 or text[i-1]==" "):
               count+=1
        return count
    text=input("Enter a string:")
    count=count_words(text)
    print(f"The count of words in a string:{count}")

s20.def longest_word(text):
    current_word = ""
    longest = ""
    for letter in text:
        if letter != " ":
            current_word+=letter
        else:
            if len(current_word) > len(longest):
                longest = current_word
            current_word = ""
    if len(current_word) > len(longest):
        longest = current_word
    return longest

text = input("Enter a string: ")
result = longest_word(text)
print(f"The longest word is: {result}")


s20.def shortest_word(text):
    current_word = ""
    shortest = None
    for letter in text:
        if letter != " ":
            current_word+=letter
        else:
            if shortest is None or len(current_word) < len(shortest)  :
                shortest = current_word
            current_word = ""
    if shortest is None or len(current_word) < len(shortest) :
        shortest = current_word
    return shortest

text = input("Enter a string: ")
result = shortest_word(text)
print(f"The shortest word is: {result}")

s21.def shortest_word(text):
    current_word = ""
    shortest = None
    for letter in text:
        if letter != " ":
            current_word+=letter
        else:
            if shortest is None or len(current_word) < len(shortest)  :
                shortest = current_word
            current_word = ""
    if shortest is None or len(current_word) < len(shortest) :
        shortest = current_word
    return shortest

text = input("Enter a string: ")
result = shortest_word(text)
print(f"The shortest word is: {result}")


s22.def reverse_each_word(text):
    current_word = ""
    final = ""

    for letter in text:
        if letter != " ":
            current_word += letter
        else:
            reverse_word = ""
            for ch in current_word:
                reverse_word = ch + reverse_word
            final += reverse_word + " "
            current_word = ""

    reverse_word = ""
    for ch in current_word:
        reverse_word = ch + reverse_word
    final += reverse_word

    return final

text = input("Enter a string: ")
result = reverse_each_word(text)
print("The reversed string is:", result)


s23.def capitalize_words(text):
        letter1=""
        letter2=""
        final=""
        for i in range(len(text)):
          if text[i]  in "abcdefghijklmnopqrstuvwxyz" and (i == 0 or text[i-1] == " "):      
            letter1=ord(text[i])
            letter2=letter1-32
            final+=chr(letter2)
          else:
            final+=text[i]
        return final
text=input("Enter a string:")
final=capitalize_words(text)
print(f"The capitalized sentence:{final}")
            
s24.def count_words_starting_with_vowel(text):
        count=0               
        for i in range(len(text)):
            if text[i]!=" " and( i == 0 or text[i-1]==" ") and text[i] in "aeiouAEIOU":
               count+=1
        return count
text=input("Enter a string:")
count=count_words_starting_with_vowel(text)
print(f"The count of words starting with vowels in a string:{count}")



s25.def count_words_starting_with_consonant(text):
        count=0         
        alphabets = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ"      
        for i in range(len(text)):
            if text[i]!=" " and( i == 0 or text[i-1]==" ") and text[i] not in "aeiouAEIOU" and text[i] in alphabets:
               count+=1
        return count
text=input("Enter a string:")
count=count_words_starting_with_consonant(text)
print(f"The count of words starting with consonant in a string:{count}")

s26.def count_words_ending_with_vowel(text):
        count=0
        for i in range(len(text)):
            if text[i]!=" " and text[i] in "aeiouAEIOU" and(i == len(text) - 1 or text[i+1]==" ") :
               count+=1
        return count
text=input("Enter a string:")
count=count_words_ending_with_vowel(text)
print(f"The count of words ending with vowel in a string:{count}")


s27.def count_words_ending_with_consonant(text):
        count=0
        alphabets = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ" 
        for i in range(len(text)):
            if text[i]!=" " and text[i] not in "aeiouAEIOU" and text[i] in alphabets and(i == len(text) - 1 or text[i+1]==" ") :
              count+=1
        return count
text=input("Enter a string:")
count=count_words_ending_with_consonant(text)
print(f"The count of words ending with consonant in a string:{count}")


s28.def first_word(text):
        first_word=""
        for i in range(len(text)):
            if text[i]!=" ":
                first_word+=text[i] 
            else:
                break

        return first_word
text=input("Enter a string:")
first_word=first_word(text)
print(f"The first word of the string :{first_word}")


s29.def last_word(text):
        last=""
        current_word=""
        for letter in text:
            if letter != " ":
               current_word += letter

            else:
               last = current_word
               current_word = ""

        if current_word != "":
           last = current_word
            
        return last
text=input("Enter a string:")
last=last_word(text)
print(f"The last word of the string :{last}")

s30.def replace_spaces(text):
        replace=""
        for letter in text:
            if letter!= " ":
               replace+=letter

            else :
               replace+="-"
        return replace
text=input("Enter a string:")
replace=replace_spaces(text)
print(f"The updated string :{replace}")

s31.def replace_vowels(text):
        vowel="aeiouAEIOU"
        final=""
        for letter in text:
            if letter in vowel:
               final+="*"
            else:
               final+=letter
        return final
text=input("Enter a string:")
final=replace_vowels(text)
print(f"The updated string :{final}")

s32.def replace_extra_space(text):
        replace=""
        for i in range(len(text)):
            if i>0 and text[i]==" " and text[i-1]==" " :
                continue
            else:
               replace+=text[i]
        return replace
text=input("Enter a string:")
replace=replace_extra_space(text)
print(f"The updated string :{replace}")
                  
s33.def is_anagram(text1, text2):
      if len(text1) != len(text2):
        return False
      for letter in text1:
        count1 = 0
        count2 = 0
        for ch in text1:
          if ch == letter:
            count1 += 1
        for ch in text2:
          if ch == letter:
            count2 += 1
        if count1 != count2:
           return False
      return True
text1=input("Enter a string1:")
text2=input("Enter a string2:")
result=is_anagram(text1,text2)
print(f"The two strings are anagram:{result}")

s34.def character_frequency(text):
       printed=""
       for letter in text:
          count=0
          if letter in printed:
               continue
          for ch in text:
                if letter==ch:
                      count+=1
          print(letter,":",count)
          printed+=letter

text=input("Enter a string:")
result=character_frequency(text)
print(f"Character frequency:{result}")


s35.def rotation(text1,text2):
      if len(text1) == len(text2) and text2 in (text1 + text1):
           return True
      return False      
text1=input("Enter a string1:")
text2=input("Enter a string2:")
result=rotation(text1,text2)
print(f"Character rotation:{result}")

              






